<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="/main.css" />
  </head>
  <body class="grey-bg">
    <%- include('nav.ejs') %>

    <div class="detail-bg">
      <h4><%= result.title %></h4>
      <p><%= result.content %></p>
      <img src="<%= result.img %>" alt="" />

      <a href="/chat/request?writerId=<%= result.user %>">채팅하기</a>

      <hr style="margin-top: 60px" />
      <div>
        <p><strong>작성자</strong> 댓글내용</p>
        <% for(let i = 0; i < commentResult.length; i++){ %>
        <p>
          <strong><%= commentResult[i].writer %></strong> <%=
          commentResult[i].content %>
        </p>
        <% } %>
      </div>
      <form id="commentForm" action="/comment" method="POST">
        <input name="content" />
        <input hidden name="parentId" value="<%= result._id %>" />
        <button type="submit">댓글작성</button>
      </form>
    </div>

    <!-- <a href="/update/<%= result._id %>">수정</a> -->
  </body>
</html>
